"""qualis UI — a local browser app over the data-quality journey.

A *driving adapter* (hexagonal): it holds zero domain logic and calls the
same application services the CLI does. Optional install: ``qualis[ui]``.
See docs/agdr/AgDR-0009-ui-framework-fastapi-htmx.md.
"""
